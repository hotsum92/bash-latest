if [ -z "$1" ]; then
  for file in *; do
    echo $file
    exit 0
  done
else
  for dir in $@; do
    if [ ! -d $dir ]; then
      continue
    fi

    dir=$(echo $dir | sed 's/\/$//')

    for file in $dir/*; do
      echo $file
      break
    done
  done
fi

